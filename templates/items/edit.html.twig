{% extends 'base.html.twig' %}

{% block title %}Edit Items{% endblock %}

{% block body %}
    <h1 class="text-light">Edit Items</h1>

    {{ include('items/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_items_index') }}">Back to list</a>

    {{ include('items/_delete_form.html.twig') }}
{% endblock %}
