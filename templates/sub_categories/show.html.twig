{% extends 'base.html.twig' %}

{% block title %}SubCategories{% endblock %}

{% block body %}
    <h1 class="text-light">SubCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th class="text-light">Id</th>
                <td class="text-light">{{ sub_category.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sub_categories_index') }}">back to list</a>

    {# <a href="{{ path('app_sub_categories_edit', {'id': sub_category.id}) }}">edit</a> #}

    {# {{ include('sub_categories/_delete_form.html.twig') }} #}
{% endblock %}
